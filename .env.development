# Used automatically when you run: npm run dev
# Axios will call http://127.0.0.1:5000/api/... directly (works no matter dev port — 3000, 5173, etc.).
# Start the backend first: npm run dev (inside /back).
VITE_API_BASE_URL=http://127.0.0.1:5000
